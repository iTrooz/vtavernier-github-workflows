# docs: skip

name: Check workflow repository

on:
  push: {}

jobs:
  check-workflows:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: yarn install --frozen-lockfile
      - run: yarn ci:check
